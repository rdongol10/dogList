import { DogPrefixPipe } from './dog-prefix.pipe';

describe('DogPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new DogPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
